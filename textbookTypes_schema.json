{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "textbookType_schema.json",
  "name": "textbook-like resource types",
  "description": "required and allowed types for learning resources that are textbooks",
  "type": "array",
  "minItems": 1,
  "description": "must say what rdf class resource is",
  "contains": {
    "type": "string",
    "format": "regex",
    "pattern": "LearningResource",
    "description": "required: must be a schema.org/LearningResource"
  },
  "items": {
    "type": "string",
    "uniqueItems": true,
    "enum": [
      "LearningResource",
      "Book",
      "Chapter",
      "Course"
    ],
    "description": "A very limit list of schema.org types relevant to textbooks & similar."
  }
}
